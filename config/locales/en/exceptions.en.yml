en:
  exceptions:
    authenticable_errors:
      unauthorized_token: Invalid or missing authentication token.
      forbidden_token: Access forbidden for this token.

    nostr_errors:
      missing_private_key: A private key must be configured as `NOSTR_PRIVATE_KEY` ENV var to publish to Nostr.
      missing_relay_url: A relay URL must be configured as `NOSTR_RELAY_URL` ENV var to publish to Nostr.
      publication_error: Note could not be published to any Nostr relay.

    merchant:
      not_found: The requested merchant does not exist
    directory:
      not_found: The requested directory entry does not exist
